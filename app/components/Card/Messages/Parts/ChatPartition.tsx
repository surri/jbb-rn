import React from 'react'
import styled from 'styled-components/native'
import { Partition as styledPartition } from '../../../Themed'

const ChatPartition = () => (
    <Partition />
)

const Partition = styled(styledPartition)`
    height: 0.4px;
`
export default ChatPartition